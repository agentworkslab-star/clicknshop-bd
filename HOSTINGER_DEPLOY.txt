═══════════════════════════════════════════════════
 ClickNShop.bd - HOSTINGER DEPLOYMENT GUIDE
═══════════════════════════════════════════════════

PROJECT LOCATION (use this exact folder):
E:\Web Dasbord ai\Poject\BanglaWriter

═══════════════════════════════════════════════════
 STEP 1: CREATE ZIP FROM MAIN PROJECT
═══════════════════════════════════════════════════

⚠️ CRITICAL: Boss-এর ZIP-এ files missing কারণ:
   ❌ Wrong folder selected (parent instead of contents)
   ❌ node_modules included (makes zip HUGE)
   ❌ Stale zip used

✅ CORRECT process:

Step 1a: Open File Explorer
Step 1b: Address bar: E:\Web Dasbord ai\Poject\BanglaWriter
Step 1c: Press Enter (you should be INSIDE BanglaWriter folder)
Step 1d: VERIFY: Should see app/, components/, lib/, prisma/, etc.

Step 1e: Press Ctrl+A
   ⚠️ SELECT EVERYTHING INSIDE THE FOLDER (NOT the folder itself)
   ⚠️ This selects files like: app, components, lib, package.json, etc.

Step 1f: Right-click → Send to → Compressed (zipped) folder

Step 1g: Name: ClickNShop-v1.zip

Step 1h: ⏳ Wait 1-2 minutes

Step 1i: ⚠️ VERIFY ZIP SIZE < 5 MB
   ❌ If 50+ MB → node_modules included → REDO
   ✅ If < 5 MB → continue

Step 1j: ⚠️ EXTRACT TEST:
   1. Right-click ClickNShop-v1.zip → "Extract All"
   2. Extract to: C:\test\
   3. Open C:\test\ and VERIFY:
      ✅ app/ folder exists
      ✅ components/ui/button.tsx EXISTS
      ✅ components/ui/input.tsx EXISTS
      ✅ components/ui/label.tsx EXISTS
      ✅ package.json exists
   4. If button/input/label.tsx MISSING in extracted folder → redo ZIP

═══════════════════════════════════════════════════
 STEP 2: RUN SQL IN NEON (create database tables)
═══════════════════════════════════════════════════

Boss Browser-এ:
1. https://console.neon.tech/app/projects/summer-sun-05670628
2. Click "SQL Editor" tab (left sidebar)
3. Open file: E:\Web Dasbord ai\Poject\BanglaWriter\init-db-now.sql
   ⚠️ USE THIS FILE — not anything else
4. Select All (Ctrl+A), Copy (Ctrl+C)
5. Paste in Neon SQL Editor (Ctrl+V)
6. Click "Run" button
7. ⏳ Wait 30-60 seconds
8. Should see: "Tables created with seed data!" OR error message

═══════════════════════════════════════════════════
 STEP 3: SET ENVIRONMENT VARIABLES IN HOSTINGER
═══════════════════════════════════════════════════

Boss Browser-এ: https://hpanel.hostinger.com
1. Login
2. Click "Websites" (left sidebar)
3. Click your website (navajowhite-snake-854284)
4. Click "Settings and redeploy" tab
5. Scroll to "Environment Variables" section
6. Click "Add Variable" button (7 times, once per variable)

Add these EXACT 7 variables:

┌───────────────────────────────────────────────────────────┐
│ Variable Name    │ Value                                          │
├──────────────────┼────────────────────────────────────────────────┤
│ DATABASE_URL     │ postgresql://neondb_owner:***@ep-rapid-star-atd0slal-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
│ GROQ_API_KEY     │ gsk_xxx (Boss's actual key from saved values)
│ NEXTAUTH_SECRET  │ zWnR3HTncx5eMSNeq4gDynEsAuDjpgDuTRz41VPABUY=
│ ENCRYPTION_KEY   │ 2506edbe3540de35a35cbddb4420b491
│ BYPASS_MODE_ENABLED │ true
│ NEXTAUTH_URL     │ https://navajowhite-snake-854284.hostingersite.com
│ NODE_ENV         │ production
└───────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════
 STEP 4: UPLOAD AND DEPLOY
═══════════════════════════════════════════════════

1. Click "Deployments" tab (in Hostinger)
2. Inside "Settings and redeploy" section
3. Find "Source files" or similar
4. Click "Upload new files"
5. Select ClickNShop-v1.zip
6. Wait for upload (1-2 minutes)

⚠️ If Hostinger asks "Replace existing files?" → YES, replace

7. Click "Save and redeploy" button
8. ⏳ Wait for build (2-3 minutes)

═══════════════════════════════════════════════════
 STEP 5: MONITOR BUILD LOG
═══════════════════════════════════════════════════

Should see:
  ✔ Generated Prisma Client
  ▲ Next.js 14.2.35
     Creating an optimized production build ...
   ✓ Compiled successfully
  Route (app) — 16 routes listed
  Deployment completed

═══════════════════════════════════════════════════
 STEP 6: VERIFY DASHBOARD
═══════════════════════════════════════════════════

Visit: https://navajowhite-snake-854284.hostingersite.com/dashboard

Expected: "স্বাগতম, FashionBD!" + 4 stat cards + sidebar

═══════════════════════════════════════════════════
 IF BUILD STILL FAILS:
═══════════════════════════════════════════════════

Boss paste FULL build log here → I will diagnose.

Most likely causes:
❌ ZIP-এ button.tsx/input.tsx/label.tsx missing → REDO ZIP
❌ DATABASE_URL not set → ADD env var
❌ ZIP has node_modules → REDO (size 50+ MB)
❌ Supabase file/folders still in ZIP → DELETE lib/supabase/

═══════════════════════════════════════════════════
 EMERGENCY DIAGNOSTIC (if files truly missing):
═══════════════════════════════════════════════════

Run this in PowerShell to verify files exist:

Get-ChildItem "E:\Web Dasbord ai\Poject\BanglaWriter\components\ui" | Format-Table Name, Length

Should show 18 files. If button.tsx/input.tsx/label.tsx missing → file system issue → check if files are hidden or corrupted.

═══════════════════════════════════════════════════
