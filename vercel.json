{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cp prisma/schema.production.prisma prisma/schema.prisma && prisma generate && prisma db push --accept-data-loss --skip-generate && tsx prisma/seed.ts || true && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "regions": ["sin1"],
  "github": {
    "silent": false
  },
  "env": {
    "BYPASS_MODE_ENABLED": "true"
  }
}
